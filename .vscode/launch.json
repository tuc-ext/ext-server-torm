{
  "version": "0.2.0",
  "configurations": [
    {
        "type": "node",
        "request": "launch",
        "name": "Launch/自启动",
        "cwd": "${workspaceFolder}",
        "program": "${workspaceFolder}/server.js",
//          "args": [], // param for script
//          "runtimeArgs": "" // param for node itself
    }
  ]
}